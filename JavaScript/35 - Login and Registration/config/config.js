/* Default Configuration for establishing Connection in Database */
const dbConfig = {
    host: "localhost",
    user: "root",
    password: "",
    database: "log_reg_js",
};

module.exports = dbConfig;
